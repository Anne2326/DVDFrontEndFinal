import { FilterCusPipe } from './filter-cus.pipe';

describe('FilterCusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCusPipe();
    expect(pipe).toBeTruthy();
  });
});
